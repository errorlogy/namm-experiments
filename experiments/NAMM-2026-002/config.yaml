experiment_id: NAMM-2026-002
domain: rewriting
rewriting_max_length: 6
rewriting_num_rules: 3
confluence_threshold: 1.0
num_candidates: 40
seed: 2026
representation_ratio_threshold: 2.0
research_question: >
  Can search discover a confluent string rewriting system on bounded {a,b}
  strings that random rule generation cannot find, with certificate-first
  artifacts and representation asymmetry K_A/K_H >= 2?
baselines:
  - sort-ba-ab
  - random_rules
